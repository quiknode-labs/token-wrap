/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    assertAccountExists,
    assertAccountsExist,
    combineCodec,
    decodeAccount,
    fetchEncodedAccount,
    fetchEncodedAccounts,
    getAddressDecoder,
    getAddressEncoder,
    getStructDecoder,
    getStructEncoder,
    type Account,
    type Address,
    type EncodedAccount,
    type FetchAccountConfig,
    type FetchAccountsConfig,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type MaybeAccount,
    type MaybeEncodedAccount,
} from '@solana/kit';
import { CanonicalPointerSeeds, findCanonicalPointerPda } from '../pdas';

/** The unwrapped mint authority's selected canonical Token Wrap deployment. */
export type CanonicalDeploymentPointer = { programId: Address };

export type CanonicalDeploymentPointerArgs = CanonicalDeploymentPointer;

/** Gets the encoder for {@link CanonicalDeploymentPointerArgs} account data. */
export function getCanonicalDeploymentPointerEncoder(): FixedSizeEncoder<CanonicalDeploymentPointerArgs> {
    return getStructEncoder([['programId', getAddressEncoder()]]);
}

/** Gets the decoder for {@link CanonicalDeploymentPointer} account data. */
export function getCanonicalDeploymentPointerDecoder(): FixedSizeDecoder<CanonicalDeploymentPointer> {
    return getStructDecoder([['programId', getAddressDecoder()]]);
}

/** Gets the codec for {@link CanonicalDeploymentPointer} account data. */
export function getCanonicalDeploymentPointerCodec(): FixedSizeCodec<
    CanonicalDeploymentPointerArgs,
    CanonicalDeploymentPointer
> {
    return combineCodec(getCanonicalDeploymentPointerEncoder(), getCanonicalDeploymentPointerDecoder());
}

export function decodeCanonicalDeploymentPointer<TAddress extends string = string>(
    encodedAccount: EncodedAccount<TAddress>,
): Account<CanonicalDeploymentPointer, TAddress>;
export function decodeCanonicalDeploymentPointer<TAddress extends string = string>(
    encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CanonicalDeploymentPointer, TAddress>;
export function decodeCanonicalDeploymentPointer<TAddress extends string = string>(
    encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CanonicalDeploymentPointer, TAddress> | MaybeAccount<CanonicalDeploymentPointer, TAddress> {
    return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getCanonicalDeploymentPointerDecoder());
}

export async function fetchCanonicalDeploymentPointer<TAddress extends string = string>(
    rpc: Parameters<typeof fetchEncodedAccount>[0],
    address: Address<TAddress>,
    config?: FetchAccountConfig,
): Promise<Account<CanonicalDeploymentPointer, TAddress>> {
    const maybeAccount = await fetchMaybeCanonicalDeploymentPointer(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}

export async function fetchMaybeCanonicalDeploymentPointer<TAddress extends string = string>(
    rpc: Parameters<typeof fetchEncodedAccount>[0],
    address: Address<TAddress>,
    config?: FetchAccountConfig,
): Promise<MaybeAccount<CanonicalDeploymentPointer, TAddress>> {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeCanonicalDeploymentPointer(maybeAccount);
}

export async function fetchAllCanonicalDeploymentPointer(
    rpc: Parameters<typeof fetchEncodedAccounts>[0],
    addresses: Array<Address>,
    config?: FetchAccountsConfig,
): Promise<Account<CanonicalDeploymentPointer>[]> {
    const maybeAccounts = await fetchAllMaybeCanonicalDeploymentPointer(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}

export async function fetchAllMaybeCanonicalDeploymentPointer(
    rpc: Parameters<typeof fetchEncodedAccounts>[0],
    addresses: Array<Address>,
    config?: FetchAccountsConfig,
): Promise<MaybeAccount<CanonicalDeploymentPointer>[]> {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map(maybeAccount => decodeCanonicalDeploymentPointer(maybeAccount));
}

export function getCanonicalDeploymentPointerSize(): number {
    return 32;
}

export async function fetchCanonicalDeploymentPointerFromSeeds(
    rpc: Parameters<typeof fetchEncodedAccount>[0],
    seeds: CanonicalPointerSeeds,
    config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<CanonicalDeploymentPointer>> {
    const maybeAccount = await fetchMaybeCanonicalDeploymentPointerFromSeeds(rpc, seeds, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}

export async function fetchMaybeCanonicalDeploymentPointerFromSeeds(
    rpc: Parameters<typeof fetchEncodedAccount>[0],
    seeds: CanonicalPointerSeeds,
    config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<CanonicalDeploymentPointer>> {
    const { programAddress, ...fetchConfig } = config;
    const [address] = await findCanonicalPointerPda(seeds, { programAddress });
    return await fetchMaybeCanonicalDeploymentPointer(rpc, address, fetchConfig);
}
